import { createBrowserRouter } from "react-router-dom";

const Route = createBrowserRouter([
  {
    path: "/",
    element: <p className="text-center text-4xl">Home</p>,
  },
]);

export default Route;
